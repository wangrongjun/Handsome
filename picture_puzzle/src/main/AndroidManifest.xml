<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wang.picture_puzzle">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity android:name=".select_picture.SelectPictureActivity" />
        <activity
            android:name=".play_game.PlayGameActivity"
            android:configChanges="orientation" />
        <activity
            android:name=".play_game.ShowPictureActivity"
            android:theme="@style/trans_style" />

    </application>

</manifest>
